from pathlib import Path


def replace(path, old, new):
    p = Path(path)
    text = p.read_text()
    if old not in text:
        raise RuntimeError(f"missing fragment in {path}: {old[:80]!r}")
    p.write_text(text.replace(old, new, 1))

# Toolbar border persistence.
p = "app/src/main/java/com/dessalines/thumbkey/ui/components/keyboard/ToolbarThemePreferences.kt"
replace(p, 'private const val KEY_MEDIA_URI = "media_uri"\n', 'private const val KEY_MEDIA_URI = "media_uri"\nprivate const val KEY_BORDER_WIDTH = "border_width"\nprivate const val KEY_BORDER_COLOR = "border_color"\n')
# Keep border prefs separate from backdrop state to avoid disturbing upstream-ish state models.
append = '''\nobject ToolbarBorderPreferences {\n    private const val PREFS_NAME = "birdie_toolbar_border"\n    private const val WIDTH = "width"\n    private const val COLOR = "color"\n\n    fun loadWidth(context: Context): Float =\n        context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).getFloat(WIDTH, 0.6f)\n\n    fun saveWidth(context: Context, value: Float) {\n        context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit().putFloat(WIDTH, value.coerceIn(0f, 4f)).apply()\n    }\n\n    fun loadColor(context: Context): Color =\n        Color(context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).getInt(COLOR, Color(0xFFFFC247).toArgb()))\n\n    fun saveColor(context: Context, value: Color) {\n        context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE).edit().putInt(COLOR, value.toArgb()).apply()\n    }\n}\n'''
Path(p).write_text(Path(p).read_text() + append)

# Draw only top/bottom toolbar hairlines.
p = "app/src/main/java/com/dessalines/thumbkey/ComposeKeyboardView.kt"
replace(p, 'import androidx.compose.foundation.layout.Column\n', 'import androidx.compose.foundation.Canvas\nimport androidx.compose.foundation.layout.Column\n')
replace(p, 'import androidx.compose.ui.graphics.Color\n', 'import androidx.compose.ui.graphics.Color\nimport androidx.compose.ui.graphics.drawscope.Stroke\n')
replace(p, 'import com.dessalines.thumbkey.ui.components.keyboard.ToolbarThemePreferences\n', 'import com.dessalines.thumbkey.ui.components.keyboard.ToolbarBorderPreferences\nimport com.dessalines.thumbkey.ui.components.keyboard.ToolbarThemePreferences\n')
old = '''                                SuggestionBarV2(ctx)\n                            }\n'''
new = '''                                SuggestionBarV2(ctx)\n                                val toolbarBorderWidth = ToolbarBorderPreferences.loadWidth(ctx)\n                                val toolbarBorderColor = ToolbarBorderPreferences.loadColor(ctx)\n                                if (toolbarBorderWidth > 0f) {\n                                    Canvas(modifier = Modifier.fillMaxWidth().height(42.dp)) {\n                                        val stroke = toolbarBorderWidth.dp.toPx()\n                                        drawLine(toolbarBorderColor, start = androidx.compose.ui.geometry.Offset(0f, stroke / 2f), end = androidx.compose.ui.geometry.Offset(size.width, stroke / 2f), strokeWidth = stroke)\n                                        drawLine(toolbarBorderColor, start = androidx.compose.ui.geometry.Offset(0f, size.height - stroke / 2f), end = androidx.compose.ui.geometry.Offset(size.width, size.height - stroke / 2f), strokeWidth = stroke)\n                                    }\n                                }\n                            }\n'''
replace(p, old, new)

# Advanced UI: imports and toolbar border controls + compact saved-gradient selector.
p = "app/src/main/java/com/dessalines/thumbkey/ui/components/settings/lookandfeel/AdvancedLookAndFeelScreen.kt"
replace(p, 'import androidx.compose.material3.Button\n', 'import androidx.compose.material3.Button\nimport androidx.compose.material3.DropdownMenu\nimport androidx.compose.material3.DropdownMenuItem\nimport androidx.compose.material3.OutlinedTextField\n')
replace(p, 'import com.dessalines.thumbkey.ui.components.keyboard.FontPreferences\n', 'import com.dessalines.thumbkey.ui.components.keyboard.FontPreferences\nimport com.dessalines.thumbkey.ui.components.keyboard.GradientLibrary\nimport com.dessalines.thumbkey.ui.components.keyboard.SavedGradient\n')
replace(p, 'import com.dessalines.thumbkey.ui.components.keyboard.ToolbarThemePreferences\n', 'import com.dessalines.thumbkey.ui.components.keyboard.ToolbarBorderPreferences\nimport com.dessalines.thumbkey.ui.components.keyboard.ToolbarThemePreferences\n')
replace(p, '''                SurfaceThemeSection(\n                    title = "Suggestion toolbar",\n                    subtitle = "The strip behind suggestion lozenges and the ✨ toggle.",\n                    load = ToolbarThemePreferences::load,\n                    save = ToolbarThemePreferences::save,\n                )\n''', '''                SurfaceThemeSection(\n                    title = "Suggestion toolbar",\n                    subtitle = "The strip behind suggestion lozenges and the ✨ toggle.",\n                    load = ToolbarThemePreferences::load,\n                    save = ToolbarThemePreferences::save,\n                    showToolbarBorder = true,\n                )\n''')
replace(p, '''    save: (android.content.Context, BackdropThemeState) -> Unit,\n) {\n''', '''    save: (android.content.Context, BackdropThemeState) -> Unit,\n    showToolbarBorder: Boolean = false,\n) {\n''')
# Insert border UI after subtitle.
replace(p, '''        Text(subtitle, style = MaterialTheme.typography.bodySmall)\n        ChipRow(\n''', '''        Text(subtitle, style = MaterialTheme.typography.bodySmall)\n        if (showToolbarBorder) {\n            var borderWidth by remember { mutableStateOf(ToolbarBorderPreferences.loadWidth(context)) }\n            var borderColorText by remember {\n                mutableStateOf(String.format("#%08X", ToolbarBorderPreferences.loadColor(context).value.toLong()))\n            }\n            Text("Toolbar border: ${String.format("%.1f", borderWidth)} dp", style = MaterialTheme.typography.titleSmall)\n            Slider(\n                value = borderWidth,\n                onValueChange = { value ->\n                    borderWidth = value\n                    ToolbarBorderPreferences.saveWidth(context, value)\n                },\n                valueRange = 0f..3f,\n            )\n            OutlinedTextField(\n                value = borderColorText,\n                onValueChange = { text ->\n                    borderColorText = text\n                    runCatching { Color(android.graphics.Color.parseColor(text)) }.getOrNull()?.let { color ->\n                        ToolbarBorderPreferences.saveColor(context, color)\n                    }\n                },\n                label = { Text("Border color (#AARRGGBB or #RRGGBB)") },\n                singleLine = true,\n                modifier = Modifier.fillMaxWidth(),\n            )\n        }\n        ChipRow(\n''')
# Replace colorful preset/editor block with managed selector while retaining editor.
start = '''        if (state.mode == BackdropMode.COLORFUL) {\n            Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {\n                listOf(BackdropPreset.BIRDIE_RAINBOW, BackdropPreset.SINEBOW).forEach { preset ->\n                    FilterChip(\n                        selected = state.preset == preset,\n                        onClick = {\n                            val selectedPreset = BackdropThemePreferences.stateForPreset(preset)\n                            persist(\n                                state.copy(\n                                    preset = preset,\n                                    angleDegrees = selectedPreset.angleDegrees,\n                                    stops = selectedPreset.stops,\n                                ),\n                            )\n                        },\n                        label = {\n                            Text(if (preset == BackdropPreset.BIRDIE_RAINBOW) "Birdie Rainbow" else "Sinebow")\n                        },\n                    )\n                }\n            }\n            GradientEditor("Gradient", state.toBackdrop()) { gradient ->\n                persist(\n                    state.copy(\n                        preset = BackdropPreset.CUSTOM,\n                        angleDegrees = gradient.angleDegrees,\n                        stops = gradient.stops,\n                    ),\n                )\n            }\n        }\n'''
managed = '''        if (state.mode == BackdropMode.COLORFUL) {\n            var gradients by remember { mutableStateOf(GradientLibrary.load(context)) }\n            var menuOpen by remember { mutableStateOf(false) }\n            var selectedName by remember { mutableStateOf("Current gradient") }\n            var saveName by remember { mutableStateOf("") }\n\n            Text("Saved gradient", style = MaterialTheme.typography.titleMedium)\n            Box {\n                Button(onClick = { menuOpen = true }, modifier = Modifier.fillMaxWidth()) {\n                    Text("$selectedName  ▾")\n                }\n                DropdownMenu(expanded = menuOpen, onDismissRequest = { menuOpen = false }) {\n                    gradients.forEach { saved ->\n                        DropdownMenuItem(\n                            text = { Text(saved.name) },\n                            onClick = {\n                                selectedName = saved.name\n                                menuOpen = false\n                                persist(\n                                    state.copy(\n                                        preset = BackdropPreset.CUSTOM,\n                                        angleDegrees = saved.angleDegrees,\n                                        stops = saved.stops,\n                                    ),\n                                )\n                            },\n                        )\n                    }\n                    DropdownMenuItem(\n                        text = { Text("＋ New gradient") },\n                        onClick = {\n                            selectedName = "New gradient"\n                            menuOpen = false\n                            persist(\n                                state.copy(\n                                    preset = BackdropPreset.CUSTOM,\n                                    angleDegrees = 0f,\n                                    stops = listOf(\n                                        KeyboardGradientStop(0f, Color(0xFF151A2C)),\n                                        KeyboardGradientStop(1f, Color(0xFFB5D8FF)),\n                                    ),\n                                ),\n                            )\n                        },\n                    )\n                }\n            }\n            GradientEditor("Gradient", state.toBackdrop()) { gradient ->\n                persist(\n                    state.copy(\n                        preset = BackdropPreset.CUSTOM,\n                        angleDegrees = gradient.angleDegrees,\n                        stops = gradient.stops,\n                    ),\n                )\n            }\n            Row(horizontalArrangement = Arrangement.spacedBy(8.dp), verticalAlignment = Alignment.CenterVertically) {\n                OutlinedTextField(\n                    value = saveName,\n                    onValueChange = { saveName = it },\n                    label = { Text("Gradient name") },\n                    singleLine = true,\n                    modifier = Modifier.weight(1f),\n                )\n                Button(\n                    enabled = saveName.isNotBlank(),\n                    onClick = {\n                        val saved = GradientLibrary.saveCustom(\n                            context,\n                            SavedGradient(\n                                id = "",\n                                name = saveName.trim(),\n                                angleDegrees = state.angleDegrees,\n                                stops = state.stops,\n                            ),\n                        )\n                        selectedName = saved.name\n                        saveName = ""\n                        gradients = GradientLibrary.load(context)\n                    },\n                ) { Text("Save") }\n            }\n            Text(\n                "Saved gradients use portable Keywi JSON; import/export controls are the next library action.",\n                style = MaterialTheme.typography.bodySmall,\n            )\n        }\n'''
replace(p, start, managed)
