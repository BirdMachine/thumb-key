# Context Engine next steps

- Smart Enter: route IME action vs newline through shared capabilities.
- Selection transforms: Advanced Characters and ✨ Tools should require `canTransformSelection`.
- Structured token extraction: keep email/URL/handle behavior behind the shared policy toggle.
- Per-app profiles can be layered onto `ContextEngineSettings` later without changing consumers.
